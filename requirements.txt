# Prompt Engineering Evaluator — Python dependencies
# Tested with Python 3.10+
# Last updated: June 2026
#
# ──────────────────────────────────────────────────────────────────
# SECURITY — CVE MITIGATIONS (do NOT downgrade langchain-core):
#
#   CVE-2025-68664 (CVSS 9.3)  "LangGrinch" — Fixed in langchain-core>=1.2.5
#   CVE-2026-34070 (CVSS 7.5)  path traversal — Fixed in langchain-core>=1.2.22
#   CVE-2026-44843              unsafe deserialization — Fixed in langchain-core>=1.3.3
#
#   All three are patched by pinning >=1.4.8 (current: 1.4.8). ✓
#   Additional mitigation: this codebase never calls load()/loads() on
#   LLM output, never sets secrets_from_env=True, and never calls
#   load_prompt() with user-controlled paths.
# ──────────────────────────────────────────────────────────────────

# LangChain — LCEL chains, ChatGroq integration
langchain>=1.3.11
langchain-core>=1.4.8
langchain-groq>=1.1.2

# Pydantic v2 — structured output schemas with field-level validation
pydantic>=2.0.0,<3.0.0

# Environment variable management — secrets stay out of code
python-dotenv>=1.0.0

# Rich — pretty console output (tables, panels, colour-coded scores)
rich>=13.0.0
